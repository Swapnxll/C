#include <stdio.h>
#include <stdlib.h>
int sum(int a, int b);
int d;
int main() 
{
   d= sum(938,5);
    printf("the value of n is %d \n", d);
}
int sum(int a, int b) { //a and b are parameters and 2,3 are arguments
    int c;
    c=a+b;
    
    //why this code is working if there is no return 
}